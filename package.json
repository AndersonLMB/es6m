{
  "name": "es6m",
  "version": "1.0.0",
  "description": "",
  "main": "i.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel src -d dist",
    "browserify": "browserify dist/index.js > i.js",
    "build": "npm run babel && npm run browserify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "browserify": "^16.2.2"
  }
}